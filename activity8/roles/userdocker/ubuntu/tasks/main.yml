---
# tasks file for roles/userdocker/ubuntu
- name: Create docker group
  become: yes
  group:
    name: docker
    state: present

- name: Add the docker group to current user
  become: yes
  user:
    name: jalopez-tip
    group: docker
